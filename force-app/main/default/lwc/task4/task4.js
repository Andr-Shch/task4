import { LightningElement, api, track, wire } from 'lwc';


const FIELDS = ['Name', 'IsEligible__c', 'Status__c'];

export default class ShowAccount extends LightningElement {
    @api strRecordId;
  
    


    
}